
class Server(object):

    def __init__(self, system, local_id, num_nodes):
        self._system = system
        self._local_id = local_id
        self._num_servers = num_servers


    def start(self):
        """Execute a server; runs forever."""

        while True:
            pass
